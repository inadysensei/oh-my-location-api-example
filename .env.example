# Shared secret. Must match the Access token in the Oh My Location iOS app.
OML_BEARER_TOKEN=change-me

# Host port for the API (container always listens on 8080).
OML_PORT=8080

# PostgreSQL (used by docker compose)
POSTGRES_USER=oml
POSTGRES_PASSWORD=oml
POSTGRES_DB=oml
